import { UserComplaintService } from './user-complaint.service';

describe('UserComplaint', () => {
  it('should create an instance', () => {
    expect(new UserComplaintService()).toBeTruthy();
  });
});
